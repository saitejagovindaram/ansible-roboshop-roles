---
- name: "Setup {{component}} for Roboshop"
  hosts: "{{component}}"
  # hosts: localhost
  become: yes
  tasks:
    - name: "Running {{component}} role"
      ansible.builtin.import_role:
        name: "{{component}}"